services:
  - type: web
    name: docu-ai
    runtime: python
    pythonVersion: "3.12"
    buildCommand: pip install -r requirements.txt
    startCommand: reflex run --env prod --backend-host 0.0.0.0 --backend-port $PORT
    envVars:
      - key: GROQ_API_KEY
        sync: false
